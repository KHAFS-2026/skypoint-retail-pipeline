name: retail
version: 1.0.0
config-version: 2

profile: retail

model-paths: ["models"]
macro-paths: ["macros"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  retail:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
